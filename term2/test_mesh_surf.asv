x=[-10:0.1:10];
y=x;
[x,y]=meshgrid(x,y);
z=sin((x.^2+y.^2).^0.5)./(x.^2+y.^2).^0.5;
surf(x,y,z);

x=[-10:0.1:10];
y=x;
[x]
for i=1:length(x)
    for j=1:length(y)
    z=sin(sqrt(x(i)^2+y(i)^2))/sqrt(x(i)^2+y(i)^2);
end
