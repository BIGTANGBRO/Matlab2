%plot nyquist diagram
clear
clc

num = [2 5 1];
den = [1 2 3];
s=tf('s');
sys=(2)*(3)/((2*s+1)^2);
% sys = tf(num,den);
syms x;
fx=(x-1)*(x-4)/((x+1)*(x+4));
ans = diff(fx);
pretty(ans)


figure(1)
nyquist(sys);
figure(2)
rlocus(sys);
% figure(3)
% bode(sys);
